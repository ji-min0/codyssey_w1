services:
  web:
    build: .
    container_name: my-web
    ports:
      - "8080:80"
    environment:
      - SERVICE_NAME=web
    depends_on:
      - cache

  cache:
    image: redis:alpine
    container_name: my-cache
